# HITO-003 — Sistema visual Vanguard y navegación mobile

## Estado

Implementación completada y verificada en el árbol de trabajo. Se
aplicaron las decisiones V1–V10: tema Vanguard, Poppins, Material Community
Icons, navegación `Home | vacío | Catálogos | Perfil`, back raíz interceptado,
cambio de contraseña obligatorio sin back y Perfil sin react-native-paper.

## Evidencia actual

- Tema, tokens, componentes base y fuentes en `mobile/src/theme`,
  `mobile/src/components` y `mobile/android/app/src/main/assets/fonts`.
- `Home`, `Catálogos` y `Perfil` registrados en `RootNavigator`.
- Versión sincronizada en package, Gradle y `APP_VERSION`: `1.2.0`, código 3.
- Historial visible actualizado en `mobile/versionHistory.js`.
- `git diff --check`: sin errores.
- `npm install --package-lock-only --ignore-scripts`: lockfile sincronizado.

## Verificación de cierre

- `npm run lint -- --no-fix`: PASS, exit 0.
- `npx tsc --noEmit`: PASS, exit 0.
- `npm test -- --runInBand --forceExit`: PASS, 7 suites / 27 tests.
- `gradlew.bat assembleRelease --no-daemon`: PASS, APK release reconstruido;
    `versionName 1.2.0`, `versionCode 3`.
- Auditoría integral: PASS técnico, 0 críticos y 0 altos.
- El commit único de cierre queda preparado por el Orchestrator.
