# HITO-003 — Sistema visual Vanguard y navegación mobile

## Estado

**CERRADO** — 2026-08-19. Implementación completada y verificada en el árbol de
trabajo. Se aplicaron las decisiones V1–V10: tema Vanguard, Poppins, Material
Community Icons, navegación `Home | (vacío) | Catálogos | Perfil`, back raíz
interceptado, cambio de contraseña obligatorio sin back y Perfil sin
react-native-paper.

## Alcance implementado

- **Commit `9a1bf0f` (base)**: tema, tokens, 14 componentes base (AppButton,
  AppInput, AppCard, AppHeader, AppIconButton, StatusChip, BottomNavigation,
  ConfirmDialog, EmptyState, LoadingState, ErrorState, ErrorBoundary, etc.),
  fuentes Poppins + MaterialCommunityIcons, ServerCheck/Home/Catálogos/Perfil
  (versión mínima), bump 1.2.0 / versionCode 3, lockfile y red security config.
- **Delta (working tree, sin commit previo)**: LoginScreen corregido (bug 3
  doble toque con `keyboardShouldPersistTaps="handled"` + SafeArea + theme),
  CambiarPasswordScreen (SafeArea + theme + BackHandler), SettingsScreen y
  PlaceholderScreen re-themaedos, PerfilScreen elevado (avatar, historial
  completo, ConfirmDialog, padding insets), HomeScreen (ConfirmDialog logout +
  0 hardcodes + BackHandler), BackHandler en las 4 raíces (V6), test Home
  actualizado (Ley 5), splash con token.
- **Remediación H-01..H-06 (auditoría)**: BackHandler en Login (H-01),
  SafeArea en Home (H-02), slot vacío de BottomNavigation en posición 2 (H-03),
  tabla markdown corregida (H-04), AMEND entrada 1.2.0 en versionHistory (H-05),
  doc-comment ServerCheck (H-06).

## Evidencia final

- `mobile/src/theme/*` y `mobile/src/components/*` (tokens + componentes base).
- `Home | (vacío) | Catálogos | Perfil` registrados en `RootNavigator` +
  `BottomNavigation` (slot vacío posición 2, sin icono/label/navegación).
- Versión sincronizada: `package.json` = `src/constants/appVersion.ts` =
  `build.gradle` (`1.2.0` / `versionCode 3`) = `output-metadata.json` = top de
  `mobile/versionHistory.js` (entrada 1.2.0 ampliada con los fixes).
- `git diff --check`: sin errores (solo avisos CRLF).

## Verificación de cierre (re-ejecutada por el auditor)

- `npm run lint -- --no-fix`: PASS, exit 0.
- `npx tsc --noEmit`: PASS, exit 0.
- `npm test -- --runInBand --forceExit`: PASS, 7 suites / 27 tests.
- `gradlew.bat assembleRelease --no-daemon`: PASS, APK release reconstruido
  (`mobile/android/app/build/outputs/apk/release/app-release.apk`, fecha
  2026-08-19 21:30:03, ~65,5 MB); `versionName 1.2.0`, `versionCode 3`.
- Auditoría integral: **PASS** (0 críticos / 0 altos / 0 medios pendientes;
  1 observación documental H-08 resuelta en el cierre).
- Commit único de cierre ejecutado por el Orchestrator.
