/**
 * appVersion.ts — Versión visible del aplicativo mobile (HITO-003, Ley 3).
 *
 * Debe coincidir EXACTAMENTE con `package.json` (version 1.5.0),
 * `android/app/build.gradle` (versionName 1.5.0 / versionCode 6) y con la
 * entrada `1.5.0` de `versionHistory`. Se expone en PerfilScreen.
 */
export const APP_VERSION = '1.5.0';