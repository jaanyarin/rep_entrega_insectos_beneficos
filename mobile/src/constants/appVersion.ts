/**
 * appVersion.ts — Versión visible del aplicativo mobile (HITO-003, Ley 3).
 *
 * Debe coincidir EXACTAMENTE con `package.json` (version 1.8.0),
 * `android/app/build.gradle` (versionName 1.8.0 / versionCode 11) y con la
 * entrada `1.8.0` de `versionHistory`. Se expone en PerfilScreen.
 */
export const APP_VERSION = '1.8.0';